title: DivisionStandings
description: >
  Standings group for a league (and optionally a division). Pre-division eras
  may omit division.
type: object
required:
  - league
  - sport
  - teamRecords
properties:
  standingsType:
    type: string
  league:
    $ref: ./schemas.yaml#/League
  division:
    $ref: ./schemas.yaml#/Division
  sport:
    $ref: ./schemas.yaml#/Sport
  roundRobin:
    title: RoundRobin
    type: object
    properties:
      status:
        type: string
  lastUpdated:
    type: string
  teamRecords:
    type: array
    items:
      $ref: ./schemas.yaml#/Record
