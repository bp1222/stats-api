get:
  operationId: getRosterTypes
  summary: List roster types
  description: >
    Returns roster type metadata; use parameter values as rosterType query params.
  tags:
    - Reference
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: ../components/schemas/schemas.yaml#/RosterType
