# /v1/windDirection
get:
  operationId: getWindDirections
  summary: List wind directions
  description: >
    Returns wind direction codes.
  tags:
    - Reference
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: ../components/schemas/schemas.yaml#/WindDirection
