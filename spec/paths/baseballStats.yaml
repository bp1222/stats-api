# /v1/baseballStats
get:
  operationId: getBaseballStats
  summary: List baseball stats
  description: >
    Returns baseball stat definitions (names, lookup params, groups).
  tags:
    - Reference
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: ../components/schemas/schemas.yaml#/HighLowType
