get:
  operationId: getPitchTypes
  summary: List pitch types
  description: >
    Returns pitch type codes and descriptions.
  tags:
    - Reference
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: ../components/schemas/schemas.yaml#/PitchType
